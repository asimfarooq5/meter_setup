name: meter_setup
description: "MeterSet Pro - Electricity Meter Reading Editor"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  share_plus: ^10.0.3
  shared_preferences: ^2.3.4
  url_launcher: ^6.3.1
  google_fonts: ^6.2.1
  google_mobile_ads: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
